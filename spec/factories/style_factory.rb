FactoryBot.define do
  factory :style do
    name { "Black and White" }
    description { "Color omitted, but not the awesome-ness. Black and White styling utilizes grey-scale (mixing white into black) to achieve shading and textures." }
  end
end
